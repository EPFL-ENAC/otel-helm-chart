apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "otel.fullname" . }}-collector
  labels:
    {{- include "otel.labels" . | nindent 4 }}
    app.kubernetes.io/component: collector
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "otel.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: collector
  template:
    metadata:
      labels:
        {{- include "otel.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: collector
      annotations:
        configmap-hash: {{ include (print $.Template.BasePath "/cm-otel-collector.yaml") . | sha256sum }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.collector.serviceAccount }}
      serviceAccountName: {{ .Values.collector.serviceAccount }}
      {{- end }}
      containers:
        - name: otel-collector
          image: "{{ .Values.collector.image.repository }}:{{ .Values.collector.image.tag }}"
          args:
            - "--config=/conf/collector.yaml"
          ports:
            - containerPort: {{ .Values.collector.receivers.grpc.port }}
            - containerPort: {{ .Values.collector.exporters.port }}
          volumeMounts:
            - name: config
              mountPath: /conf
          resources:
            {{- toYaml .Values.collector.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ include "otel.fullname" . }}-collector-config
